<!DOCTYPE html>
<html>

<head>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    <style>
        ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            overflow: hidden;
            background-color: #333;
        }
        
        li {
            float: left;
        }
        
        li a {
            display: block;
            color: white;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
        }
        
        li a:hover {
            background-color: #111;
        }
        
        .btn-block {
            margin-top: 10px;
            text-align: center;
            max-width: 10%;
        }
        
        button {
            width: 100%;
            padding: 10px 0;
            margin: 10px auto;
            border-radius: 5px;
            border: none;
            background: #74c58f;
            font-size: 14px;
            font-weight: 600;
            color: #fff;
        }
    </style>
</head>

<body>
    <ul>
        <li>
            <a class="active">
                <%= email %>
            </a>
        </li>
        <li><a href="/user/logout">Logout</a></li>
    </ul>
    <div class="btn-block">
        <form method="POST" action="#">
        <% for (var i = 0; i < groups.length ; i++) { %>
            <button type="submit" value="${groups[i]}"  name="action"><a href='dashboard/group'> <%= groups[i] %> </a></button>
            <% } %>
        </form>
    </div>
</body>

</html>